## 调研命题

{seed}

## 反馈信息

{feedback}

{review_history_summary}

---

请基于以上调研命题生成调研报告。

**如果是第一轮（无反馈信息）**：
- 按照「第一轮工作模式」严格执行：阶段一规划 → 阶段二搜索 + 生成 → 阶段三自检
- 阶段二必须先 web_search 获取数据，再基于搜索结果生成报告
- 每条数据标注来源层级 🔍/📚/💡
- 阶段三必须 read_file 回读刚写入的文件，逐项检查来源覆盖和完整性
- 自检通过后才能结束

**如果是后续轮次（有反馈信息）**：
- 请先用 read_file 读取 {output_file} 了解当前报告内容
- 按照「修复工作模式」三阶段执行：阶段一计划 → 阶段二分步修改 → 阶段三自检
- 对每个阻塞性问题，解释你在报告中如何修复了它
- 对每条改进建议，说明你是采纳还是拒绝，并简要说明理由
- 确保报告始终围绕原始调研命题，不要因修补反馈而偏离方向
- 三个阶段完毕后将最终报告写入 {output_file}